#!/usr/bin/env bash

# remove all old generated code
rm src/conf/*

# compile the XML Schema into C++ code
xsdcxx cxx-tree \
       --std c++11 \
       --hxx-suffix .hpp \
       --cxx-suffix .cpp \
       --output-dir src/conf \
       ../schema.xsd
