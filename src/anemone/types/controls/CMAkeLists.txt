set(control_types_header_files ${CMAKE_CURRENT_LIST_DIR}/ppqn.hpp)
add_library(control_types ${control_types_header_files})

target_include_directories(
  control_types
  PUBLIC
  ${CMAKE_CURRENT_LIST_DIR})
